CREATE OR REPLACE FUNCTION FN_CALCULAREDAD (P_FECHA_NACIMIENTO DATE)
RETURN NUMBER
IS V_EDAD NUMBER;
BEGIN IF P_FECHA_NACIMIENTO IS NULL THEN
            RETURN NULL;
END IF;

V_EDAD := FLOOR(MONTHS_BETWEEN(SYSDATE,P_FECHA_NACIMIENTO)/12);
 RETURN V_EDAD;
END;
 